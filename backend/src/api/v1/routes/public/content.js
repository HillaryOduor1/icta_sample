/*const express = require('express');
const router = express.Router();
const { getContentByPage } = require('../../controllers/contentController');

const { getAllContent } = require('../../controllers/contentController');

router.get('/', getAllContent);      // returns all content
// Example: /api/public/content/home
router.get('/:page', getContentByPage);

module.exports = router;*/